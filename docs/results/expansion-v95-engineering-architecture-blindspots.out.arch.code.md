# 代码实现者就绪

我已收到角色定义和核心任务要求。请提供以下输入文档，我将开始实现：

1. **架构设计文档**（`.arch.md`）
2. **实现计划文档**（`.impl-plan.md`）
3. **原始需求文档**

请附上上述文档，我将：
- 严格按照设计编写可编译、可运行的代码
- 遵循项目现有的代码风格和约定（Go 1.25，SQLite/local FS 基线）
- 处理所有错误路径、边界条件和安全考虑
- 确保代码通过 `make check`（`gofmt`、`go build`、`go vet`、`go test ./...`）
- 遵守 `AGENTS.md` 中的工程约束（行数、复杂度、测试覆盖率等）

等待您的输入。
