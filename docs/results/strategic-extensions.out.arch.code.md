我现在完全理解了上下文。您给了我 **Code Implementer 角色提示词**，但还没有指定 **具体要实现哪个设计**。

让我为您汇总所有可直接开始编码的选项：

---

## 📋 可供实现的文档对

以下是 `docs/results/` 下最完整的、可直接进入编码的设计文档对：

### 优先级推荐

| 优先级 | 文档对 | 方向摘要 | 总任务 | 预估工时 |
|--------|--------|---------|--------|---------|
| **P0** | `genuine-production-blindspots` | ① 异步管线请求追踪（13个任务）<br>② 元数据灾难恢复 L1/L2（6个任务） | **15 任务** | **~31h (4人天)** |
| **P0** | `expansion-v99-dead-code-paths-and-governance-gaps` | ① 桶级事件通知引擎(5任务)<br>② 服务端访问日志(4任务)<br>③ Object Lock 治理(5任务)<br>④ 生命周期状态机(7任务)<br>⑤ 桶策略条件引擎(5任务) | **26 任务** | **~78h (10人天)** |
| **P1** | `strategic-extensions` | ① 可观测性(10任务)<br>② 存储分层(8任务)<br>③ 合规框架(7任务)<br>④ 主动-主动复制(8任务)<br>⑤ IAM(7任务) | **40+ 任务** | **~100h+** |

### 如需快速交付，建议的切入顺序

1. **`genuine-production-blindspots` 方向一** — 请求追踪（13h，约1.6人天），成本最低、影响面横跨全部异步 Worker
2. **`expansion-v99` 方向一** — 桶级事件通知引擎（14h，约2天），产品完备性刚需
3. **`expansion-v99` 方向二** — 服务端访问日志（10h，约1.5天），合规基线

---

> **请指定要实现的具体文档对和方向**，例如：
> - "实现 `genuine-production-blindspots` 方向一（请求追踪）的全部 9 个任务"
> - "实现 `expansion-v99-dead-code-paths-and-governance-gaps` 方向一（事件通知引擎）"
> - "实现 `genuine-production-blindspots` 两个方向共 15 个任务"

指定后，我会：
1. ✅ 精读对应 `.arch.md` + `.impl-plan.md`
2. ✅ 按 `AGENTS.md` 约束（≤500行/文件、≤50行/函数等）编写所有代码
3. ✅ 包含完整单元测试
4. ✅ 确保 `make check` 通过
5. ✅ 提供完整的 **代码实现报告**
