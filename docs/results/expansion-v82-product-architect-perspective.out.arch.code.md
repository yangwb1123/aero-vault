你好！我已经理解了你的 **Code Implementer** 角色要求。

在正式开始实现之前，我需要你提供以下输入文件：

1. **架构设计文档**（`.arch.md`）— 系统的架构设计
2. **实现计划文档**（`.impl-plan.md`）— 具体的实现步骤和计划
3. **原始需求文档** — 要实现的功能或需求的详细描述

请提供这些文档（或直接粘贴内容），我将严格按照以下流程执行：

```
接收设计文档 → 理解架构约束 → 按实现计划编码 → 验证编译/测试 → 提交报告
```

我注意到当前项目是 **aero-vault**（Go 1.25 项目），我会严格遵循 `AGENTS.md` 中定义的工程约束：

| 规则 | 阈值 |
|------|------|
| 单文件 ≤ 500 行 | ✓ |
| 单函数 ≤ 50 行 | ✓ |
| 圈复杂度 ≤ 10 | ✓ |
| 禁止 `utils/` `common/` 包 | ✓ |
| 测试覆盖率 ≥ 50% | ✓ |
| 变更后运行 `make check` | ✓ |

同时也遵循硬性不变量（SQL 占位符不可复用 I1、迁移双文件 I2、存储 key 唯一 I3 等）。

请提供你的需求/设计文档，我立即开始实现！
