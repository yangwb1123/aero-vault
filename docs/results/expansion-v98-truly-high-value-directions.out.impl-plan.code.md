I've reviewed the current workspace. The TASK.md shows the bootstrap context chain is complete, and the CURRENT_SPRINT.md shows the integration validation sprint is done.

There are many `.arch.md` and `.impl-plan.md` files in `docs/results/` but none is explicitly designated as the active implementation target.

To proceed, I need you to specify:

1. **Which feature/module** do you want me to implement? Please provide the specific:
   - **Architecture design** (`.arch.md`) 
   - **Implementation plan** (`.impl-plan.md`)
   - **Requirements** (if separate)

2. **Any priority directives** — e.g., is there a specific sprint or expansion that's currently active?

For example, I see many options like `expansion-v120-bootstrap-migration-graceful-shutdown`, `expansion-v119-strategic-extension-directions`, etc. Which one should I work on?
