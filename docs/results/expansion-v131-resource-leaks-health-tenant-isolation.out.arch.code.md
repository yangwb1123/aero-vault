I've reviewed the current workspace context. Here's what I found:

**Current State:**
- **TASK.md** — all tasks marked as complete ✅
- **CURRENT_SPRINT** (end-to-end integration verification) — all items complete ✅
- **Latest HEAD** — `[pi-batch] Stage: technical-design` — a batch that generated many `.arch.md` and `.impl-plan.md` files
- **No pending `.code.md` files** from the latest batch (the previous batch did produce `.code.md` output files)

There are **many architecture plans** recently generated (e.g., `high-value-extensions-v21`, `expansion-v33-fuse-custody`, `expansion-v29-architecture-blindspots`, etc.), each with detailed task breakdowns.

However, you've only provided the **Code Implementer role prompt** without specifying:
1. **Which architecture document** (`.arch.md`) to follow
2. **Which implementation plan** (`.impl-plan.md`) to execute
3. **Which specific task(s)** to implement

Could you please clarify what you'd like me to implement? For example:
- A specific `.impl-plan.md` file and task ID(s) from it?
- A custom requirement not covered by existing plans?
- Or should I pick a priority item from the latest batch and propose an approach?
