好的，我已详细阅读了项目文档。我注意到当前环境中的 `docs/results/` 目录和 `docs/requirements/` 目录包含大量扩展分析文档，但由于某些文件访问限制，我无法直接读取文件来确认具体需要实现的内容。

请提供您希望我实现的具体任务。您可以选择以下方式之一：

---

### 选项 1：已有的架构设计 + 实现计划（基于 ROADMAP 内容）

根据 `ROADMAP.md`，以下方向已有分析但可能尚未实现代码：

| 方向 | 描述 | 状态 |
|------|------|------|
| **#6** | 生产弹性（熔断器、bulkhead、超时） | 尚未实现 |
| **#7** | S3 特性对等（bucket policy、CORS、logging、通知等） | 部分未实现 |
| **#8** | 内容完整性 & 自愈（checksum、数据巡检） | 尚未实现 |
| **#9** | 存储分层 & 智能生命周期（storage class、tier transition） | 尚未实现 |
| **#10** | 元数据 HA & 灾难恢复（read replica、PITR） | 尚未实现 |

### 选项 2：指定具体的 `.arch.md` + `.impl-plan.md` 文件

如果您有现成的架构设计和实现计划（位于 `docs/results/` 下），请告诉我文件路径。

### 选项 3：粘贴需求/设计内容

直接在此处粘贴需求或设计文档。

---

**请告知您要实现的具体内容，我将严格按照架构设计 + 实现计划编写代码，并确保：**
- 代码可编译、可运行
- 遵循项目现有代码风格（Go 1.25、chi/v5、SQLite/local FS 基线）
- 遵守工程约束（单文件 ≤ 500 行、单函数 ≤ 50 行、圈复杂度 ≤ 10）
- 包含完整测试
- 通过 `make check`
