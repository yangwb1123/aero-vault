Here's the current state of the project:

- **Root**: `/home/u1/aero-vault` — a Go project (`aero-vault`)
- **Modified files**: Many files have pending changes (see git status)
- **Staged files**: None currently staged

But I still need **specific implementation requirements** to proceed. You've only provided the role prompt template.

Please share any of the following:

1. **A task description** — what needs to be implemented or changed
2. **An architecture design doc** (`.arch.md`) — if one exists
3. **An implementation plan** (`.impl-plan.md`)
4. **Raw requirements** — feature description, user stories, etc.

For example, are you looking to:
- Implement a new feature (e.g., a new storage backend, a new API endpoint, a new worker)?
- Refactor an existing module?
- Fix bugs or add tests?
- Something from the `docs/agent/` directory?
