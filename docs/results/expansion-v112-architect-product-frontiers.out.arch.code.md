# 代码实现报告

您好！我准备好将技术设计转化为代码实现了。但在我开始之前，我需要以下输入来完成我的工作：

## 请提供以下文档

### 必需的输入
1. **架构设计文档** — `.arch.md` 文件
2. **实现计划文档** — `.impl-plan.md` 文件  
3. **原始需求文档** — 说明需要实现什么功能

### 或者直接告诉我
- **需要实现什么功能**（例如：一个新的 Storage Backend、一个 REST handler、一个 MCP tool 等）
- **涉及哪些模块**（参考项目架构：`internal/service`, `internal/api/rest`, `internal/storage`, `internal/repository` 等）

---

## 项目上下文（已加载）

我已经加载了项目 `github.com/aero-vault/aero-vault`（Go 1.25）的 AGENTS.md，了解以下全局约束：

| 约束 | 阈值 |
|------|------|
| 单文件 ≤ 500 行 | ✓ |
| 单函数 ≤ 50 行 | ✓ |
| 圈复杂度 ≤ 10 | ✓ |
| 禁止 God 类型（超 300 行） | ✓ |
| 禁止 `utils/` `common/` `helper/` 包 | ✓ |
| 所有业务逻辑必须可测试 | ✓ |
| 测试覆盖率 ≥ 50% | ✓ |

**CI Gate：** `gofmt -l .` → `go build ./...` → `go vet ./...` → `go test ./...`

## 下一步

请提供设计文档或直接描述要实现的功能，我将：
1. 仔细阅读架构设计和实现计划
2. 按渐进式实现原则编写代码
3. 确保通过 CI Gate 检查
4. 生成完整的代码实现报告
