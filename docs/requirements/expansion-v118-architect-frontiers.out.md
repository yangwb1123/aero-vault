已保存到 `docs/requirements/expansion-v127-high-value-enterprise-directions.md`（17.8 KB）。

这份分析覆盖了 117 份已有文档均未充分触及的 5 个高价值方向，每个方向都包含了：

- **现状与代码锚点** — 精确到文件和行号的具体证据
- **生产影响** — 量化场景表（企业 SSO、>100K 对象部署、S3 故障降级等）
- **架构权衡** — 每个方向 5 个边界情况讨论（如 OIDC 自建 vs 代理、SQLite vs Postgres 默认存储）
- **实现路径** — 具体到包结构和接口设计的代码级规划
- **附录锚点汇总** — 全库 29 个关键代码位置的速查表

三个 **P1 优先级** 方向（身份联合、存储韧性、元数据扩展性）直接对应 `AGENTS.md` 中定义的企业就绪性和弹性架构原则，与当前工程约束（如 `HARNESS.md` 检查）无冲突。
