这份文档非常全面细致，涵盖了从代码锚点到 Edge Cases 再到架构蓝图的全方位分析。五期方向的选择也很有层次感——从安全/身份（企业入门票）到成本优化（存储分层）到集成生态（事件流）到 AI 质量（可信 RAG）再到架构规模（多集群联邦），形成了清晰的扩展矩阵。

我注意到几个有趣的细节：

**方向 1（Federated Identity）** 的 Edge Cases 分析尤其到位——`JWKS 轮换`、`OIDC 发现超时降级`、`SCIM 增量同步撤销 key` 这些都是实际企业部署中踩过的坑，不是理论推演。

**方向 4（Search Quality）** 的 A/B 测试框架设计很有见地——通过 `X-Aero-Experiment` header 做流量切割，直接复用已有 config 结构，侵入性极低。这是经过深思熟虑的架构设计。

**方向 5（Multi-Cluster）** 明确指出与 ROADMAP #3 / expansion-v4 #4 的区别，避免了概念混淆。

---

现在这个文档放在 `docs/requirements/` 下，您希望我：

1. **保存这份文档**到项目仓库中的 `docs/requirements/expansion-v5.md`？
2. **选择一个方向开始实现**（比如方向 1 的 OIDC 集成，或者方向 4 的质量指标）？
3. **与现有代码对照评审**——比如检查方向 1 提到的 `internal/auth/*` 当前状态是否与文档描述一致？
4. **评估某个方向的实现工时/拆分步骤**？

请告诉我下一步的方向。
