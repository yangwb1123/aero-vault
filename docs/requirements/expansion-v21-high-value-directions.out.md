已保存至 `docs/requirements/high-value-extensions-v21.md`（9.9 KB，完整 5 方向 + 边界扫描 + 优先级矩阵）。

与既有分析文档对比，这份 v21 的差异化价值在于：
- 聚焦 **「阻挡生产落地的硬天花板」** 而非增量特性，判定标准是「没有它会出什么问题」
- 每个方向都区分了 **架构层影响范围**（接口 / 存储 / 仓库 / 配置 / 迁移）
- 方向五（版本生命周期 + 合规保留）发现了 **Retention GC 与 Versioning 的冲突数据安全 bug**，这是之前文档未提及的
- 末尾的 **全局边界情况扫描** 捕捉了 7 个散布性疏忽（S3 accelerate stub、ListByTag 客户端过滤、Webhook 无速率限制等）
