已保存为 `docs/requirements/expansion-v130-production-quality-gaps.md`（557 行，30KB），继 v129 之后的顺序编号。文档已包含完整的：

- **5 个方向**：Context 传播连续性、HTTP 连接池、优雅关闭排空、结构化错误域、测试质量基础设施
- 每个方向附带现状 → 代码锚点 → 缺失能力 → 为什么需要 → 架构概要 → 边界情况
- 优先级表、依赖关系图（Mermaid）、与现有方向的协同矩阵
- 逐方向的 `grep` 去重验证记录
