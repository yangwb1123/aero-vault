apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aero-vault.fullname" . }}
  labels:
    {{- include "aero-vault.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
